## Topics I learned today

- Markdown
- Commands in Git Bash and Powershell

### **Makrkdown**

Before we move on to the markdown files lets first look what is plain text .

Plain text is text that contains only characters letters ,numbers punctuation marks without any formatting information .

Markup languages uses a lot of syntax to describe the structure .

There are many markup language:

- HTML
- XML
- SGML

Markdown is simpler version of markup language .

I learned that we use markdown language because

- it is simpler
- it is easy to render
- it is simple to write and read.

I learned that Markdown is a plain text formatted.

It allows us to write formatted document using some symbols such as '#','-',and '\*\*'.

Also GitHub renders markdown files automatically.

Web browser doen't understand markdown therefore markdown language is rendered into html .

I learned that GitHub uses a specific version of markdown called GitHub Flavoured Markdown(GFM)

# Markdown Sheet

| Feature         | Syntax                | Example                        |
| --------------- | --------------------- | ------------------------------ |
| Heading 1       | `#`                   | `# Git`                        |
| Heading 2       | `##`                  | `## Commands`                  |
| Heading 3       | `###`                 | `### Git`                      |
| Bold            | `**text**`            | `**Git**`                      |
| Italic          | `*text*`              | `*Git*`                        |
| Bold + Italic   | `***text***`          | `***Git***`                    |
| Bullet List     | `- item`              | `- Git`                        |
| Numbered List   | `1. item`             | `1. Install Git`               |
| Nested List     | Two spaces before `-` | `  - Commit`                   |
| Task List       | `- [ ]` / `- [x]`     | `- [x] Learn Git`              |
| Inline Code     | `` `text` ``          | `` `git status` ``             |
| Code Block      | Triple backticks      | `bash ... `                    |
| Link            | `[Text](URL)`         | `[GitHub](https://github.com)` |
| Image           | `![Alt](image.png)`   | `![Logo](logo.png)`            |
| Blockquote      | `>`                   | `> Git is awesome.`            |
| Horizontal Line | `---`                 | `---`                          |

#### Resources

- [markup in 21 minutes](https://youtu.be/_PPWWRV6gbA?si=lsbwBQlPVEC3gfnT)

### **Commands in Git Bash and Powershell**

I learned basic commands equivalent in both Git Bash and Powershell and they are :

### Git Bash

- `pwd` – Shows the current working directory.
- `ls` – Lists files and folders.
- `cd` – Changes the current directory.
- `mkdir` – Creates a new directory.
- `touch` – Creates a new file.
- `cat` – Displays the contents of a file.
- `cp` – Copies a file.
- `mv` – Moves or renames a file.
- `rm` – Deletes a file.

### PowerShell

- `pwd` – Shows the current working directory.
- `ls` / `dir` – Lists files and folders.
- `cd` – Changes the current directory.
- `mkdir` – Creates a new directory.
- `New-Item` – Creates a new file.
- `Get-Content` (or `cat`) – Displays the contents of a file.
- `Copy-Item` – Copies a file.
- `Move-Item` – Moves or renames a file.
- `Remove-Item` (or `del`) – Deletes a file.

I learned commands to create folders, create files , removing files , moving files between directories ,clearing the console , copying a file form one place to another , renaming the folder.

Is there any limitation with using Git Bash on Windows , is it truly the same as using bash in Linux ?

The terminal of linux is bash and the terminal of windows are cmd and powershell .

Git was build by the creator of Linux so Git was designed to work in linux however because there are millons of developers use windows the Git Bash was made .

It is a Bash like environment on Windows (with some limited features)

I learned that Git Bash is not exactly same as using bash in linux because the commands 'chmod' exis in Git bash , but windows handles permission differently but these two are similar.

Git does have some limitation on windows, as Git bash is a shell with some linux tools on top of the Windows . For things like package managers , file permissions we can't use Linux because these things are different in Linux.
